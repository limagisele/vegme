<!DOCTYPE html>
<html>
  <head>
    <title>Vegme</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <nav class="navbar navbar-expand-md navbar-dark" style="background-color: #32a87d;">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">VegMe</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
          <ul class="navbar-nav me-auto mb-2 mb-md-0">
            <li class="nav-item">
              <%= link_to "Home", root_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Restaurants", menu_items_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Order", menu_items_path, class: "nav-link" %>
            </li>
          </ul>
          <% if user_signed_in? %>
            <div class="ml-auto btn-toolbar p-3">
              Welcome, <%= current_user.full_name %>!
            </div>
            <div class="ml-auto btn-toolbar">
              <%= button_to "Logout", destroy_user_session_path, class: "btn btn-light", method: :delete, form: { data: { turbo_confirm: "Do you want to log out?" } } %>
            </div>
          <% else %>
            <div class="ml-auto btn-toolbar p-1">
              <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-outline-light" %>
            </div>
            <div class="ml-auto btn-toolbar p-1">
              <%= link_to "Login", new_user_session_path, class: "btn btn-outline-dark" %>
            </div>
          <% end %>
        </div>
      </div>
    </nav>
    <main class="p-3">
      <% if notice %>
        <p class="alert alert-info" role="notice"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger" role="alert"><%= alert %></p>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
